import React, { FC } from 'react';
import { Seo, Layout } from 'components';

const Contact: FC = () => (
    <Layout>
        <Seo title="Contacto" />
        <h1>Contacto</h1>
    </Layout>
);

export default Contact;
